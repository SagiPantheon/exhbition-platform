"use client";

export default function TentsLayoutPage() {
  return (
    <main
      style={{
        minHeight: "100vh",
        background:
          "radial-gradient(circle at top, rgba(56,189,248,0.12), transparent 24%), linear-gradient(180deg, #050b16 0%, #040914 100%)",
        color: "#eaf4ff",
      }}
    >
      <div
        style={{
          maxWidth: "1920px",
          margin: "0 auto",
          padding: "10px 12px 12px",
          display: "grid",
          gap: "8px",
        }}
      >
        {/* 1. TOP BAR */}
        <section
          style={{
            height: "46px",
            borderRadius: "18px",
            border: "1px solid rgba(125,211,252,0.18)",
            background:
              "linear-gradient(180deg, rgba(8,16,32,0.92) 0%, rgba(6,12,24,0.98) 100%)",
            boxShadow: "0 18px 50px rgba(0,0,0,0.24)",
            display: "flex",
            alignItems: "center",
            justifyContent: "space-between",
            padding: "0 14px",
          }}
        >
          <div style={{ display: "flex", alignItems: "center", gap: "8px" }}>
            <div>
              <div
                style={{
                  fontSize: "16px",
                  fontWeight: 800,
                  letterSpacing: "0.06em",
                }}
              >
                EXHIBITION HUB
              </div>
              <div
                style={{
                  fontSize: "9px",
                  textTransform: "uppercase",
                  letterSpacing: "0.2em",
                  color: "rgba(180,220,255,0.68)",
                }}
              >
                Plan · Design · Experience
              </div>
            </div>
          </div>

          <div
            style={{
              display: "flex",
              alignItems: "center",
              gap: "18px",
              fontSize: "13px",
              color: "rgba(234,244,255,0.82)",
            }}
          >
            <span>Dashboard</span>
            <span>Planning</span>
            <span>Exhibitions</span>
            <span>Profile</span>
          </div>
        </section>

        {/* 2. CONTEXT ROW */}
        <section
          style={{
            borderRadius: "18px",
            border: "1px solid rgba(125,211,252,0.16)",
            background:
              "linear-gradient(180deg, rgba(8,16,32,0.82) 0%, rgba(6,10,22,0.94) 100%)",
            padding: "7px 10px",
            display: "flex",
            alignItems: "center",
            justifyContent: "space-between",
            gap: "16px",
            flexWrap: "wrap",
          }}
        >
          <div style={{ display: "flex", alignItems: "center", gap: "12px", flexWrap: "wrap" }}>
            <span style={{ color: "rgba(180,220,255,0.76)" }}>Layout Planning</span>
            <span style={{ color: "rgba(125,211,252,0.68)" }}>›</span>
            <strong style={{ fontSize: "18px" }}>Tent 25x15</strong>
          </div>

          <div style={{ display: "flex", gap: "8px", flexWrap: "wrap" }}>
            {["Preview", "Save", "Export Plan"].map((label, idx) => (
              <button
                key={label}
                type="button"
                style={{
                  padding: "10px 16px",
                  borderRadius: "12px",
                  border: idx === 2 ? "1px solid rgba(56,189,248,0.42)" : "1px solid rgba(148,163,184,0.22)",
                  background:
                    idx === 2
                      ? "linear-gradient(135deg, rgba(34,211,238,0.18), rgba(59,130,246,0.22))"
                      : "rgba(255,255,255,0.03)",
                  color: "#f8fbff",
                  fontWeight: 700,
                  fontSize: "13px",
                }}
              >
                {label}
              </button>
            ))}
          </div>
        </section>

        {/* 3. STEPS */}
        <section
          style={{
            borderRadius: "18px",
            border: "1px solid rgba(125,211,252,0.16)",
            background:
              "linear-gradient(180deg, rgba(7,13,26,0.86) 0%, rgba(5,10,20,0.94) 100%)",
            padding: "7px 10px",
            display: "flex",
            alignItems: "center",
            gap: "8px",
            flexWrap: "wrap",
          }}
        >
          {[
            "1. Scene Setup",
            "2. Assets & Content",
            "3. Review & Presentation",
          ].map((step, idx) => (
            <div
              key={step}
              style={{
                padding: "9px 14px",
                borderRadius: "12px",
                border:
                  idx === 0
                    ? "1px solid rgba(56,189,248,0.42)"
                    : "1px solid rgba(148,163,184,0.18)",
                background:
                  idx === 0
                    ? "linear-gradient(135deg, rgba(34,211,238,0.16), rgba(59,130,246,0.20))"
                    : "rgba(255,255,255,0.02)",
                color: idx === 0 ? "#eaf9ff" : "rgba(234,244,255,0.78)",
                fontWeight: 700,
                fontSize: "13px",
              }}
            >
              {step}
            </div>
          ))}
        </section>

        {/* 4/5/6. MAIN GRID */}
        <section
          style={{
            display: "grid",
            gridTemplateColumns: "240px minmax(0,1fr) 240px",
            gap: "8px",
            alignItems: "stretch",
          }}
        >
          {/* LEFT PANEL */}
          <aside
            style={{
              borderRadius: "22px",
              border: "1px solid rgba(125,211,252,0.16)",
              background:
                "linear-gradient(180deg, rgba(7,13,26,0.94) 0%, rgba(5,10,20,0.98) 100%)",
              padding: "12px",
              boxShadow: "0 18px 50px rgba(0,0,0,0.22)",
              minHeight: "820px",
            }}
          >
            <div style={{ fontSize: "16px", fontWeight: 800, marginBottom: "10px" }}>
              Exhibit Library
            </div>

            <div
              style={{
                display: "grid",
                gridTemplateColumns: "1fr 1fr",
                gap: "8px",
              }}
            >
              {[
                "Missile System",
                "Launcher",
                "Vehicle",
                "Radar",
                "Flag Set",
                "Screen Stand",
              ].map((item) => (
                <div
                  key={item}
                  style={{
                    borderRadius: "16px",
                    border: "1px solid rgba(148,163,184,0.16)",
                    background: "rgba(255,255,255,0.03)",
                    padding: "14px",
                    minHeight: "78px",
                    display: "flex",
                    alignItems: "center",
                    justifyContent: "space-between",
                  }}
                >
                  <span style={{ fontWeight: 700 }}>{item}</span>
                  <span style={{ color: "#7dd3fc" }}>+</span>
                </div>
              ))}
            </div>
          </aside>

          {/* CENTER SCENE */}
          <section
            style={{
              borderRadius: "26px",
              border: "1px solid rgba(125,211,252,0.18)",
              background:
                "radial-gradient(circle at center, rgba(14,165,233,0.08), transparent 36%), linear-gradient(180deg, rgba(7,13,26,0.96) 0%, rgba(5,10,20,0.98) 100%)",
              boxShadow: "0 22px 60px rgba(0,0,0,0.28)",
              padding: "12px",
              minHeight: "820px",
              position: "relative",
              overflow: "hidden",
            }}
          >
            <div
              style={{
                position: "absolute",
                inset: "0",
                backgroundImage:
                  "linear-gradient(rgba(56,189,248,0.08) 1px, transparent 1px), linear-gradient(90deg, rgba(56,189,248,0.08) 1px, transparent 1px)",
                backgroundSize: "34px 34px",
                pointerEvents: "none",
              }}
            />

            <div
              style={{
                position: "relative",
                zIndex: 1,
                display: "flex",
                justifyContent: "center",
                marginBottom: "10px",
              }}
            >
              <div
                style={{
                  display: "flex",
                  gap: "8px",
                  padding: "7px 10px",
                  borderRadius: "12px",
                  border: "1px solid rgba(148,163,184,0.18)",
                  background: "rgba(8,16,32,0.82)",
                }}
              >
                {["Select", "Move", "Rotate", "Zoom", "View"].map((tool) => (
                  <div
                    key={tool}
                    style={{
                      padding: "7px 10px",
                      borderRadius: "10px",
                      background: tool === "Select" ? "rgba(34,211,238,0.16)" : "rgba(255,255,255,0.03)",
                      border:
                        tool === "Select"
                          ? "1px solid rgba(56,189,248,0.34)"
                          : "1px solid rgba(148,163,184,0.14)",
                      fontSize: "13px",
                      fontWeight: 700,
                    }}
                  >
                    {tool}
                  </div>
                ))}
              </div>
            </div>

            <div
              style={{
                position: "relative",
                zIndex: 1,
                height: "720px",
                borderRadius: "22px",
                border: "1px solid rgba(56,189,248,0.16)",
                background:
                  "radial-gradient(circle at center, rgba(34,211,238,0.12), rgba(8,14,26,0.0) 36%), linear-gradient(180deg, rgba(6,12,24,0.84) 0%, rgba(4,8,18,0.94) 100%)",
                display: "flex",
                alignItems: "center",
                justifyContent: "center",
                overflow: "hidden",
              }}
            >
              <div
                style={{
                  width: "82%",
                  aspectRatio: "1.2 / 1",
                  borderRadius: "28px",
                  border: "2px solid rgba(56,189,248,0.65)",
                  boxShadow:
                    "0 0 0 1px rgba(125,211,252,0.14), 0 0 26px rgba(34,211,238,0.32), inset 0 0 40px rgba(34,211,238,0.08)",
                  display: "flex",
                  alignItems: "center",
                  justifyContent: "center",
                  color: "rgba(234,244,255,0.86)",
                  fontSize: "24px",
                  fontWeight: 800,
                  letterSpacing: "0.04em",
                }}
              >
                CENTER SCENE / TENT CANVAS
              </div>
            </div>
          </section>

          {/* RIGHT PANEL */}
          <aside
            style={{
              borderRadius: "22px",
              border: "1px solid rgba(125,211,252,0.16)",
              background:
                "linear-gradient(180deg, rgba(7,13,26,0.94) 0%, rgba(5,10,20,0.98) 100%)",
              padding: "12px",
              boxShadow: "0 18px 50px rgba(0,0,0,0.22)",
              minHeight: "820px",
            }}
          >
            <div style={{ fontSize: "16px", fontWeight: 800, marginBottom: "10px" }}>
              Interior & Inventory
            </div>

            <div
              style={{
                display: "grid",
                gridTemplateColumns: "1fr 1fr",
                gap: "8px",
              }}
            >
              {[
                "Table",
                "Chair",
                "Sofa",
                "Display Screen",
                "Coffee Bar",
                "Lectern",
                "Projector",
                "Speaker",
              ].map((item) => (
                <div
                  key={item}
                  style={{
                    borderRadius: "16px",
                    border: "1px solid rgba(148,163,184,0.16)",
                    background: "rgba(255,255,255,0.03)",
                    padding: "14px",
                    minHeight: "78px",
                    display: "flex",
                    alignItems: "center",
                    justifyContent: "space-between",
                  }}
                >
                  <span style={{ fontWeight: 700 }}>{item}</span>
                  <span style={{ color: "#7dd3fc" }}>+</span>
                </div>
              ))}
            </div>
          </aside>
        </section>

        {/* 7. BOTTOM STATUS */}
        <section
          style={{
            borderRadius: "18px",
            border: "1px solid rgba(125,211,252,0.16)",
            background:
              "linear-gradient(180deg, rgba(7,13,26,0.92) 0%, rgba(5,10,20,0.98) 100%)",
            padding: "7px 10px",
            display: "grid",
            gridTemplateColumns: "1.3fr repeat(5, minmax(0, 1fr)) 1.5fr",
            gap: "12px",
            alignItems: "stretch",
          }}
        >
          {[
            ["Tent 25x15", "Active template"],
            ["25m", "Length"],
            ["15m", "Width"],
            ["375m²", "Area"],
            ["42", "Elements"],
            ["96%", "Readiness"],
            ["Constraints / Recommendations / Export", "Operational status"],
          ].map(([value, label], idx) => (
            <div
              key={value + label}
              style={{
                borderRadius: "12px",
                border: "1px solid rgba(148,163,184,0.14)",
                background: idx === 5 ? "rgba(34,211,238,0.08)" : "rgba(255,255,255,0.03)",
                padding: "12px 14px",
                display: "flex",
                flexDirection: "column",
                justifyContent: "center",
                minHeight: "72px",
              }}
            >
              <div
                style={{
                  fontSize: idx === 0 ? "22px" : "24px",
                  fontWeight: 800,
                  color: idx === 5 ? "#67e8f9" : "#f8fbff",
                  marginBottom: "4px",
                }}
              >
                {value}
              </div>
              <div
                style={{
                  fontSize: "12px",
                  color: "rgba(180,220,255,0.72)",
                  textTransform: "uppercase",
                  letterSpacing: "0.08em",
                }}
              >
                {label}
              </div>
            </div>
          ))}
        </section>
      </div>
    </main>
  );
}
