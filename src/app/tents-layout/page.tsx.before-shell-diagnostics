"use client";

import { useState } from "react";

export default function TentsLayoutPage() {
  const [focusMode, setFocusMode] = useState(false);

  return (
    <main
      style={{
        minHeight: "100vh",
        background:
          "radial-gradient(circle at top, rgba(56,189,248,0.12), transparent 24%), linear-gradient(180deg, #050b16 0%, #040914 100%)",
        color: "#eaf4ff",
      }}
    >
      <div
        style={{
          maxWidth: focusMode ? "100vw" : "1960px",
          margin: "0 auto",
          padding: focusMode ? "2px 4px 4px" : "4px 8px 8px",
          display: "grid",
          gap: focusMode ? "6px" : "8px",
          transition: "all 220ms ease",
        }}
      >
        {/* 1. TOP BAR */}
        <section
          style={{
            height: focusMode ? "38px" : "46px",
            borderRadius: focusMode ? "12px" : "18px",
            border: "1px solid rgba(125,211,252,0.08)",
            background:
              "linear-gradient(180deg, rgba(8,16,32,0.92) 0%, rgba(6,12,24,0.98) 100%)",
            boxShadow: "0 18px 50px rgba(0,0,0,0.24)",
            display: "flex",
            alignItems: "center",
            justifyContent: "space-between",
            padding: focusMode ? "0 10px" : "0 14px",
            transition: "all 220ms ease",
          }}
        >
          <div style={{ display: "flex", alignItems: "center", gap: "12px", marginLeft: "118px" }}>
            <div>
              <div
                style={{
                  fontSize: focusMode ? "13px" : "16px",
                  fontWeight: 800,
                  letterSpacing: "0.06em",
                  transition: "all 220ms ease",
                }}
              >
                EXHIBITION HUB
              </div>
              {!focusMode ? (
                <div
                  style={{
                    fontSize: "9px",
                    textTransform: "uppercase",
                    letterSpacing: "0.2em",
                    color: "rgba(180,220,255,0.68)",
                  }}
                >
                  Plan · Design · Experience
                </div>
              ) : null}
            </div>
          </div>

          <div
            style={{
              display: "flex",
              alignItems: "center",
              gap: focusMode ? "10px" : "18px",
              fontSize: focusMode ? "12px" : "14px",
              color: "rgba(234,244,255,0.82)",
            }}
          >
            {!focusMode ? (
              <>
                <span>Dashboard</span>
                <span>Planning</span>
                <span>Exhibitions</span>
                <span>Profile</span>
              </>
            ) : null}

            <button
              type="button"
              onClick={() => setFocusMode((v) => !v)}
              style={{
                padding: focusMode ? "6px 12px" : "7px 10px",
                borderRadius: "999px",
                border: "1px solid rgba(56,189,248,0.34)",
                background:
                  "linear-gradient(135deg, rgba(34,211,238,0.16), rgba(59,130,246,0.20))",
                color: "#f8fbff",
                fontWeight: 800,
                fontSize: focusMode ? "12px" : "13px",
                cursor: "pointer",
              }}
            >
              {focusMode ? "Exit Focus" : "Focus Mode"}
            </button>
          </div>
        </section>

        {/* 2. CONTEXT ROW */}
        <section
          style={{
            display: focusMode ? "none" : "flex",
            borderRadius: "18px",
            border: "1px solid rgba(125,211,252,0.16)",
            background:
              "linear-gradient(180deg, rgba(8,16,32,0.82) 0%, rgba(6,10,22,0.94) 100%)",
            padding: "8px 12px",
            alignItems: "center",
            justifyContent: "space-between",
            gap: "16px",
            flexWrap: "wrap",
          }}
        >
          <div style={{ display: "flex", alignItems: "center", gap: "12px", flexWrap: "wrap" }}>
            <span style={{ color: "rgba(180,220,255,0.76)" }}>Layout Planning</span>
            <span style={{ color: "rgba(125,211,252,0.68)" }}>›</span>
            <strong style={{ fontSize: "18px" }}>Tent 25x15</strong>
          </div>

          <div style={{ display: "flex", gap: "10px", flexWrap: "wrap" }}>
            {["Preview", "Save", "Export Plan"].map((label, idx) => (
              <button
                key={label}
                type="button"
                style={{
                  padding: "8px 12px",
                  borderRadius: "12px",
                  border: idx === 2 ? "1px solid rgba(56,189,248,0.42)" : "1px solid rgba(148,163,184,0.22)",
                  background:
                    idx === 2
                      ? "linear-gradient(135deg, rgba(34,211,238,0.18), rgba(59,130,246,0.22))"
                      : "rgba(255,255,255,0.03)",
                  color: "#f8fbff",
                  fontWeight: 700,
                  fontSize: "13px",
                }}
              >
                {label}
              </button>
            ))}
          </div>
        </section>

        {/* 3. STEPS */}
        <section
          style={{
            display: focusMode ? "none" : "flex",
            borderRadius: "18px",
            border: "1px solid rgba(125,211,252,0.16)",
            background:
              "linear-gradient(180deg, rgba(7,13,26,0.86) 0%, rgba(5,10,20,0.94) 100%)",
            padding: "8px 12px",
            alignItems: "center",
            gap: "14px",
            flexWrap: "wrap",
          }}
        >
          {[
            "1. Scene Setup",
            "2. Assets & Content",
            "3. Review & Presentation",
          ].map((step, idx) => (
            <div
              key={step}
              style={{
                padding: "9px 14px",
                borderRadius: "12px",
                border:
                  idx === 0
                    ? "1px solid rgba(56,189,248,0.42)"
                    : "1px solid rgba(148,163,184,0.18)",
                background:
                  idx === 0
                    ? "linear-gradient(135deg, rgba(34,211,238,0.16), rgba(59,130,246,0.20))"
                    : "rgba(255,255,255,0.02)",
                color: idx === 0 ? "#eaf9ff" : "rgba(234,244,255,0.78)",
                fontWeight: 700,
                fontSize: "13px",
              }}
            >
              {step}
            </div>
          ))}
        </section>

        {/* 4/5/6. MAIN GRID */}
        <section
          style={{
            display: "grid",
            gridTemplateColumns: focusMode ? "1fr" : "180px minmax(0,1fr) 180px",
            gap: focusMode ? "0" : "10px",
            alignItems: "stretch",
            transition: "all 220ms ease",
          }}
        >
          {/* LEFT PANEL */}
          <aside
            style={{
              display: focusMode ? "none" : "block",
              borderRadius: "18px",
              border: "1px solid transparent",
              background:
                "linear-gradient(180deg, rgba(7,13,26,0.94) 0%, rgba(5,10,20,0.98) 100%)",
              padding: "12px",
              boxShadow: "none",
              minHeight: "900px",
            }}
          >
            <div style={{ fontSize: "18px", fontWeight: 800, marginBottom: "10px" }}>
              Exhibit Library
            </div>

            <div
              style={{
                display: "grid",
                gridTemplateColumns: "1fr 1fr",
                gap: "10px",
              }}
            >
              {[
                "Missile System",
                "Launcher",
                "Vehicle",
                "Radar",
                "Flag Set",
                "Screen Stand",
              ].map((item) => (
                <div
                  key={item}
                  style={{
                    borderRadius: "16px",
                    border: "1px solid rgba(148,163,184,0.16)",
                    background: "rgba(255,255,255,0.03)",
                    padding: "14px",
                    minHeight: "78px",
                    display: "flex",
                    alignItems: "center",
                    justifyContent: "space-between",
                    fontWeight: 700,
                  }}
                >
                  <span>{item}</span>
                  <span style={{ color: "#7dd3fc" }}>+</span>
                </div>
              ))}
            </div>
          </aside>

          {/* CENTER SCENE */}
          <section
            style={{
              borderRadius: focusMode ? "18px" : "26px",
              border: "1px solid transparent",
              background:
                "radial-gradient(circle at center, rgba(14,165,233,0.08), transparent 36%), linear-gradient(180deg, rgba(7,13,26,0.96) 0%, rgba(5,10,20,0.98) 100%)",
              boxShadow: "none",
              padding: focusMode ? "10px" : "18px",
              minHeight: focusMode ? "calc(100vh - 120px)" : "900px",
              position: "relative",
              overflow: "hidden",
              transition: "all 220ms ease",
            }}
          >
            <div
              style={{
                position: "absolute",
                inset: "0",
                backgroundImage:
                  "linear-gradient(rgba(56,189,248,0.08) 1px, transparent 1px), linear-gradient(90deg, rgba(56,189,248,0.08) 1px, transparent 1px)",
                backgroundSize: "34px 34px",
                pointerEvents: "none",
              }}
            />

            <div
              style={{
                position: "relative",
                zIndex: 1,
                display: "flex",
                justifyContent: "center",
                marginBottom: focusMode ? "8px" : "10px",
              }}
            >
              <div
                style={{
                  display: "flex",
                  gap: "10px",
                  padding: focusMode ? "6px 10px" : "7px 10px",
                  borderRadius: "12px",
                  border: "1px solid rgba(148,163,184,0.18)",
                  background: "rgba(8,16,32,0.82)",
                }}
              >
                {["Select", "Move", "Rotate", "Zoom", "View"].map((tool) => (
                  <div
                    key={tool}
                    style={{
                      padding: focusMode ? "6px 9px" : "7px 10px",
                      borderRadius: "10px",
                      background: tool === "Select" ? "rgba(34,211,238,0.16)" : "rgba(255,255,255,0.03)",
                      border:
                        tool === "Select"
                          ? "1px solid rgba(56,189,248,0.34)"
                          : "1px solid rgba(148,163,184,0.14)",
                      fontSize: focusMode ? "12px" : "13px",
                      fontWeight: 700,
                    }}
                  >
                    {tool}
                  </div>
                ))}
              </div>
            </div>

            <div
              style={{
                position: "relative",
                zIndex: 1,
                height: focusMode ? "calc(100vh - 190px)" : "800px",
                borderRadius: "18px",
                border: "1px solid transparent",
                background:
                  "transparent",
                display: "flex",
                alignItems: "center",
                justifyContent: "flex-start",
                overflow: "hidden",
              }}
            >
              <div
                style={{
                  width: focusMode ? "94%" : "74%",
                  height: focusMode ? "92%" : "82%",
                  borderRadius: "24px",
                  border: "2px solid rgba(103,232,249,0.72)",
                  boxShadow:
                    "0 0 24px rgba(34,211,238,0.24), inset 0 0 24px rgba(34,211,238,0.05)",
                  display: "flex",
                  alignItems: "center",
                  justifyContent: "center",
                  color: "rgba(234,244,255,0.86)",
                  fontSize: focusMode ? "28px" : "24px",
                  fontWeight: 800,
                  letterSpacing: "0.04em",
                }}
              >
                CENTER SCENE / TENT CANVAS
              </div>
            </div>
          </section>

          {/* RIGHT PANEL */}
          <aside
            style={{
              display: focusMode ? "none" : "block",
              borderRadius: "18px",
              border: "1px solid transparent",
              background:
                "linear-gradient(180deg, rgba(7,13,26,0.94) 0%, rgba(5,10,20,0.98) 100%)",
              padding: "12px",
              boxShadow: "none",
              minHeight: "900px",
            }}
          >
            <div style={{ fontSize: "18px", fontWeight: 800, marginBottom: "10px" }}>
              Interior & Inventory
            </div>

            <div
              style={{
                display: "grid",
                gridTemplateColumns: "1fr 1fr",
                gap: "10px",
              }}
            >
              {[
                "Table",
                "Chair",
                "Sofa",
                "Display Screen",
                "Coffee Bar",
                "Lectern",
                "Projector",
                "Speaker",
              ].map((item) => (
                <div
                  key={item}
                  style={{
                    borderRadius: "16px",
                    border: "1px solid rgba(148,163,184,0.16)",
                    background: "rgba(255,255,255,0.03)",
                    padding: "14px",
                    minHeight: "78px",
                    display: "flex",
                    alignItems: "center",
                    justifyContent: "space-between",
                    fontWeight: 700,
                  }}
                >
                  <span>{item}</span>
                  <span style={{ color: "#7dd3fc" }}>+</span>
                </div>
              ))}
            </div>
          </aside>
        </section>

        {/* 7. BOTTOM STATUS */}
        <section
          style={{
            borderRadius: "18px",
            border: "1px solid rgba(125,211,252,0.16)",
            background:
              "linear-gradient(180deg, rgba(7,13,26,0.92) 0%, rgba(5,10,20,0.98) 100%)",
            padding: focusMode ? "8px 10px" : "14px 18px",
            display: "grid",
            gridTemplateColumns: focusMode
              ? "1fr 1fr 1fr 1fr 1fr 1fr"
              : "1.3fr repeat(5, minmax(0, 1fr)) 1.5fr",
            gap: "12px",
            alignItems: "stretch",
            transition: "all 220ms ease",
          }}
        >
          {[
            ["Tent 25x15", "Active template"],
            ["25m", "Length"],
            ["15m", "Width"],
            ["375m²", "Area"],
            ["42", "Elements"],
            ["96%", "Readiness"],
          ].map(([value, label], idx) => (
            <div
              key={value + label}
              style={{
                borderRadius: "14px",
                border: "1px solid rgba(148,163,184,0.14)",
                background: idx === 5 ? "rgba(34,211,238,0.08)" : "rgba(255,255,255,0.03)",
                padding: focusMode ? "8px 10px" : "12px 14px",
                display: "flex",
                flexDirection: "column",
                justifyContent: "center",
                minHeight: focusMode ? "56px" : "72px",
              }}
            >
              <div
                style={{
                  fontSize: idx === 0 ? (focusMode ? "18px" : "22px") : (focusMode ? "20px" : "24px"),
                  fontWeight: 800,
                  color: idx === 5 ? "#67e8f9" : "#f8fbff",
                  marginBottom: "4px",
                }}
              >
                {value}
              </div>
              <div
                style={{
                  fontSize: focusMode ? "10px" : "12px",
                  color: "rgba(180,220,255,0.72)",
                  textTransform: "uppercase",
                  letterSpacing: "0.08em",
                }}
              >
                {label}
              </div>
            </div>
          ))}
        </section>
      </div>
    </main>
  );
}
